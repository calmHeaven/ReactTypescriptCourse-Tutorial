//PascalCasing -- Capitalize the first letter of each word
function Message(){
    // JSX: JavaScript XML
    const name = '';
    if (name)
    return <h1>Hello {name}</h1>
    return <h1> Hello World</h1>
}
export default Message; //Export as default object from module, so that it can be imported in other modules